---
title: Introduction
description: Zeron UI is a monorepo starter for building a component library, documentation site, and copy-paste registry together.
icon: Layers
---

Zeron UI keeps component source, documentation, examples, and registry metadata in one workspace.

## What belongs where

- Reusable components live in `packages/ui`.
- Documentation pages and previews live in `apps/docs`.
- Registry metadata lives in `packages/registry`.

## Foundation

Special thanks to Tailwind CSS, Base UI, shadcn/ui, and Fumadocs.
